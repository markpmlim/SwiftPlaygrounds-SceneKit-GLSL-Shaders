// Testing GLSL shader programs in SceneKit
// Procedural Texturing with GLSL shaders.
// This playground shows how to setup a pair of vertex-fragment
// shaders to compute a texture.
// The generated texture is then applied to shade a spherical object.
// Requirements: XCode 8.x or later

import PlaygroundSupport

let vc = SCNViewController()
PlaygroundPage.current.liveView = vc
